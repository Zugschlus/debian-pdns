For the PostgreSQL database setup.

In /usr/share/doc/pdns-backend-pgsql/ is the file pgsql.sql. This contains
a basic setup for PowerDNS.

You can add this with:
- Create first a database in PostgreSQL (For example: pdns)
- Add this file: psql -h <host> -U <user> <database> -f pgsql.sql

And then add the records to the database.

